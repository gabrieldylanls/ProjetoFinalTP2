services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      DATABASE_PATH: /data/shopping.db
      SECRET_KEY: docker-development-secret-key
      FLASK_DEBUG: "0"
      HOST: 0.0.0.0
      PORT: "5000"
    volumes:
      - shopping_data:/data

volumes:
  shopping_data:
